import styled from "styled-components";

export const MainMenuElement = styled.ul`
    display: flex;
    gap: 20px;
    width: 100%;
    flex-wrap: wrap;
    flex-direction: row;
    justify-content: center;
`