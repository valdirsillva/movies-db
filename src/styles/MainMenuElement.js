import styled from "styled-components";

export const MainMenuElement = styled.ul`
    display: flex;
    gap: 20px;
    flex-wrap: wrap;
    flex-direction: row;
    aligm-item: center;
    justify-content: center;
    margin-top: 5rem;
`