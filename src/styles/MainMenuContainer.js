import styled from "styled-components";


export const MainMenuContainer = styled.nav`
    ul {
        list-style: none;
        padding: 0;

        li {
            display: flex;
            flex-direction: column;
            gap: 20px;
        }
    }
`